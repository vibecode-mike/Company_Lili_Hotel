# 快速開始指南

## 📦 專案內容

此專案包含完整的 LINE OA 自動回應表單 React 元件,整合了 Figma 設計稿中的所有互動狀態:

1. ✅ **歡迎訊息 - Blank State** (空白初始狀態)
2. ✅ **歡迎訊息 - Editing State** (編輯狀態,含變數標籤)
3. ✅ **觸發關鍵字 - Editing State (無標籤)** (尚未輸入關鍵字)
4. ✅ **觸發關鍵字 - Editing State (標籤編輯中)** (關鍵字標籤功能)
5. ✅ **一律回應 - Editing State** (一律回應模式)

## 🚀 立即開始

### 解壓縮檔案

```bash
tar -xzf auto-response-form.tar.gz
cd auto-response-form
```

### 安裝依賴

```bash
npm install
```

### 啟動開發伺服器

```bash
npm run dev
```

專案將在 `http://localhost:5173` 運行

## 📋 專案特色

### 核心功能

- **三種回應類型**
  - 歡迎訊息:新好友加入時自動回應
  - 觸發關鍵字:偵測特定關鍵字後回應
  - 一律回應:對所有訊息都回應

- **觸發時間設定**
  - 立即回覆
  - 指定日期範圍
  - 指定時間範圍(每天)

- **關鍵字管理**
  - 支援多個關鍵字標籤(最多 20 個)
  - 按 Enter 鍵新增標籤
  - 點擊 × 移除標籤

- **訊息編輯**
  - 即時預覽
  - 變數標籤插入 `{好友的顯示名稱}`
  - 字數限制顯示(0/100)

- **多訊息管理**
  - 訊息排序切換
  - 新增/刪除訊息
  - 上一則/下一則導航

### UI/UX 設計

- ✅ 遵循 Figma 設計規範
- ✅ 使用 shadcn/ui 元件庫
- ✅ Noto Sans TC 字體
- ✅ 完整的設計系統顏色
- ✅ 響應式設計
- ✅ 無障礙支援

## 🛠️ 技術架構

```
React 18 + TypeScript
├── Vite (建置工具)
├── Tailwind CSS (樣式)
├── shadcn/ui (UI 元件)
├── Radix UI (無障礙基礎)
└── Lucide React (圖示)
```

## 📂 檔案結構

```
auto-response-form/
├── src/
│   ├── components/
│   │   ├── ui/                    # shadcn/ui 元件
│   │   └── AutoResponseForm.tsx   # 主元件
│   ├── lib/utils.ts               # 工具函數
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
└── README.md                      # 完整文檔
```

## 🎨 設計系統

### 主要顏色

- **Primary Blue**: `#0F6BEB` - 主要操作色
- **Secondary Orange**: `#EBA20F` - 標籤和提示
- **Gray Scale**: `#F5F5F5` ~ `#242424` - 各層級灰階

### 字體規格

- **標題**: 32px / Regular
- **內文**: 16px / Regular
- **說明**: 14px / Regular & Medium
- **標註**: 12px / Regular

## 💡 使用範例

### 切換回應類型

```typescript
// 在 AutoResponseForm 元件中
const handleResponseTypeChange = (value: string) => {
  setState((prev) => ({
    ...prev,
    responseType: value as ResponseType,
    keywords: [],
    keywordInput: '',
  }));
};
```

### 新增關鍵字標籤

```typescript
// 使用者按下 Enter 鍵時觸發
const handleKeywordKeyDown = (e: React.KeyboardEvent) => {
  if (e.key === 'Enter' && state.keywordInput.trim()) {
    e.preventDefault();
    addKeywordTag(state.keywordInput);
  }
};
```

### 插入變數標籤

```typescript
// 點擊「好友的顯示名稱」按鈕
const insertFriendName = () => {
  setState((prev) => ({
    ...prev,
    messageText: prev.messageText + '{好友的顯示名稱}',
  }));
};
```

## 📱 螢幕截圖

專案包含以下互動狀態:

1. **歡迎訊息 (空白狀態)**
   - 顯示初始表單
   - 單一訊息輸入框

2. **歡迎訊息 (編輯中)**
   - 多則訊息排序
   - 變數標籤顯示

3. **觸發關鍵字 (無標籤)**
   - 關鍵字輸入提示
   - 說明文字顯示

4. **觸發關鍵字 (標籤編輯中)**
   - 多個橘色關鍵字標籤
   - 標籤計數顯示

5. **一律回應**
   - 日期時間範圍選擇
   - 多則訊息內容

## 🔧 常見問題

### Q: 如何修改主題顏色?

編輯 `src/index.css` 中的 CSS 變數:

```css
:root {
  --primary: 217 89% 49%;  /* 修改主要藍色 */
  --secondary: 217 70% 88%; /* 修改次要色 */
}
```

### Q: 如何新增更多訊息欄位?

在 `AutoResponseForm.tsx` 中調整訊息數量邏輯:

```typescript
// 根據 responseType 渲染不同數量的訊息
{state.responseType === 'welcome' && (
  // 顯示 4 個訊息卡片
)}
```

### Q: 如何整合後端 API?

在元件中新增 API 呼叫函數:

```typescript
const handleSubmit = async () => {
  const response = await fetch('/api/auto-response', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(state),
  });
  // 處理回應
};
```

## 📞 支援

如有任何問題,請查閱 `README.md` 完整文檔,或聯繫開發團隊。

## 🎉 開始使用

```bash
# 1. 解壓縮
tar -xzf auto-response-form.tar.gz

# 2. 進入目錄
cd auto-response-form

# 3. 安裝依賴
npm install

# 4. 啟動專案
npm run dev

# 5. 在瀏覽器打開
# http://localhost:5173
```

祝您開發順利! 🚀
