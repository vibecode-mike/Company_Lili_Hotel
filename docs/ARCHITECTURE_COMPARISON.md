# 架构方案对比：当前方案 vs 方案 B

## 📊 当前方案（前端直接调用两个 API）

### 数据流图

```
┌─────────────────────────────────────────────────────────────┐
│                         浏览器                               │
│                                                              │
│  前端 JavaScript 代码                                         │
│  ┌────────────────────────────────────────────────────┐     │
│  │ MessagesContext.tsx:                              │     │
│  │                                                    │     │
│  │ 1. 并行调用两个 API：                              │     │
│  │    ├─→ fetch('/api/v1/messages')                 │     │
│  │    └─→ fetch('https://api-youth-tycg.star-bit.io/...') │
│  │                                                    │     │
│  │ 2. 等待两个响应                                    │     │
│  │                                                    │     │
│  │ 3. 过滤 FB 数据 (status === 1)                    │     │
│  │                                                    │     │
│  │ 4. 前端合并数据                                    │     │
│  │    allMessages = [...lineMessages, ...fbMessages] │     │
│  └────────────────────────────────────────────────────┘     │
└──────────────┬────────────────────────────────┬─────────────┘
               │                                │
               │ ① HTTP 请求                    │ ② HTTP 请求
               │                                │
               ↓                                ↓
    ┌──────────────────┐           ┌──────────────────────────┐
    │   后端 API        │           │   外部 FB API            │
    │                  │           │                          │
    │ /api/v1/messages │           │ api-youth-tycg.star-bit.io│
    │                  │           │                          │
    │ 返回:             │           │ 返回:                     │
    │ - LINE 全部       │           │ - FB 全部（包括已发送）    │
    │ - FB 草稿         │           │                          │
    │ - FB 已排程       │           │                          │
    │ - FB 发送失败     │           │                          │
    └──────────────────┘           └──────────────────────────┘
           │
           ↓
    ┌──────────────────┐
    │   数据库 (DB)     │
    │                  │
    │ - messages 表    │
    └──────────────────┘
```

### 问题

❌ **前端需要知道外部 FB API 地址**
```typescript
const fbApiBaseUrl = 'https://api-youth-tycg.star-bit.io';
```

❌ **前端需要处理 FB 登录**
```typescript
const response = await fetch(`${fbApiBaseUrl}/api/v1/admin/firm_login`, {
  body: JSON.stringify({ account: 'tycg-admin', password: '123456' })
});
```

❌ **密码暴露在前端代码中**
```javascript
// 构建后的代码
const fbFirmPassword = "123456"; // ← 用户可以在浏览器看到！
```

❌ **前端需要处理数据合并逻辑**
```typescript
// 需要过滤、转换、合并
const fbMessages = (fbResult.data || [])
  .filter((item) => item.status === 1)
  .map(transformFbBroadcastMessage);
allMessages.push(...lineMessages, ...fbMessages);
```

❌ **前端需要处理两个 API 的错误**

❌ **无法实现缓存（每次都要调用外部 API）**

---

## ✅ 方案 B（后端完全代理）

### 数据流图

```
┌─────────────────────────────────────────────────────────────┐
│                         浏览器                               │
│                                                              │
│  前端 JavaScript 代码                                         │
│  ┌────────────────────────────────────────────────────┐     │
│  │ MessagesContext.tsx:                              │     │
│  │                                                    │     │
│  │ 1. 只调用一个 API：                                │     │
│  │    const response = await apiGet('/api/v1/messages')│   │
│  │                                                    │     │
│  │ 2. 直接使用返回的数据                              │     │
│  │    setMessages(response.data.items)               │     │
│  │                                                    │     │
│  │ ✅ 不需要：                                        │     │
│  │   - 配置外部 FB API 地址                           │     │
│  │   - 处理 FB 登录                                   │     │
│  │   - 过滤和合并数据                                 │     │
│  │   - 处理多个 API 错误                              │     │
│  └────────────────────────────────────────────────────┘     │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           │ ① 只有一个 HTTP 请求
                           │
                           ↓
                ┌────────────────────────────┐
                │   后端 API                  │
                │                            │
                │  /api/v1/messages          │
                │  ┌──────────────────────┐  │
                │  │ 后端内部处理：        │  │
                │  │                      │  │
                │  │ ① 查询本地 DB        │  │
                │  │   (LINE + FB 草稿等) │  │
                │  │         │            │  │
                │  │         ↓            │  │
                │  │ ② 调用外部 FB API    │──┼─→ ③ HTTP 请求
                │  │   (获取已发送)       │  │
                │  │         │            │  │   ┌──────────────────┐
                │  │         ↓            │  │   │ 外部 FB API       │
                │  │ ③ 后端合并数据       │  │←──┤ api-youth-tycg   │
                │  │   (过滤、转换)       │  │   │                  │
                │  │         │            │  │   └──────────────────┘
                │  │         ↓            │  │
                │  │ ④ 返回统一格式       │  │
                │  └──────────────────────┘  │
                └────────────┬───────────────┘
                             │
                             ↓
                    ┌────────────────┐
                    │  数据库 (DB)    │
                    │                │
                    │ - messages 表  │
                    └────────────────┘
```

### 优点

✅ **前端超级简单**
```typescript
// 前端只需要这一行！
const response = await apiGet('/api/v1/messages');
setMessages(response.data.items);
```

✅ **密码完全隐藏在后端**
```python
# 后端 config.py（用户看不到）
FB_FIRM_PASSWORD: str = "123456"  # 从 .env 读取
```

✅ **统一错误处理**
```python
# 后端统一处理所有错误
try:
    fb_messages = await fb_client.get_sent_messages()
except Exception as e:
    logger.error(f"FB API 调用失败: {e}")
    fb_messages = []  # 降级处理
```

✅ **可以添加缓存**
```python
# 后端可以缓存 FB 数据，减少外部 API 调用
@cached(ttl=300)  # 缓存 5 分钟
async def get_sent_messages():
    return await fb_client.get_sent_messages()
```

✅ **可以添加限流**
```python
# 防止频繁调用外部 API
@rate_limit("10/minute")
async def get_sent_messages():
    ...
```

✅ **数据格式统一**
```python
# 后端保证返回统一格式，前端不需要转换
return {
    "data": {
        "items": [
            # 统一的消息格式
            {"id": 1, "platform": "LINE", ...},
            {"id": 2, "platform": "Facebook", ...},
        ]
    }
}
```

---

## 🔧 具体实现对比

### 当前方案代码量

#### 前端代码（约 80 行）
```typescript
// frontend/src/contexts/MessagesContext.tsx

// 需要配置
const jwtToken = getJwtToken();
const fbApiBaseUrl = import.meta.env.VITE_FB_API_URL;

// 需要并行调用两个 API
const fetchPromises = [
  apiGet('/api/v1/messages?page=1&page_size=100'),
];

if (jwtToken && fbApiBaseUrl) {
  fetchPromises.push(
    fetch(`${fbApiBaseUrl}/api/v1/admin/meta_page/message/gourp_list`, {
      headers: { 'Authorization': `Bearer ${jwtToken}` },
    }).catch(err => {
      console.error('FB 外部 API 調用失敗:', err);
      return new Response(JSON.stringify({ data: [] }), { status: 200 });
    })
  );
}

const [lineResponse, fbResponse] = await Promise.all(fetchPromises);

// 需要处理 LINE 数据
if (lineResponse.ok) {
  const lineResult = await lineResponse.json();
  const lineMessages = (lineResult.data?.items || []).map(transformBackendMessage);
  allMessages.push(...lineMessages);
}

// 需要处理 FB 数据
if (fbResponse?.ok) {
  const fbResult = await fbResponse.json();
  // 需要过滤
  const fbMessages = (fbResult.data || [])
    .filter((item: FbBroadcastMessage) => item.status === 1)
    .map(transformFbBroadcastMessage);
  allMessages.push(...fbMessages);
}
```

#### 后端代码（约 30 行）
```python
# backend/app/api/v1/messages.py

@router.get("")
async def get_messages(
    page: int = 1,
    page_size: int = 100,
    db: AsyncSession = Depends(get_db),
):
    """获取消息列表"""
    # 只查询本地 DB
    query = select(Message).offset(...).limit(...)
    result = await db.execute(query)
    messages = result.scalars().all()

    return {"data": {"items": [msg.to_dict() for msg in messages]}}
```

#### 配置文件
```bash
# frontend/.env.development
VITE_FB_API_URL=https://api-youth-tycg.star-bit.io
VITE_FB_FIRM_ACCOUNT=tycg-admin
VITE_FB_FIRM_PASSWORD=123456  # ⚠️ 密码暴露
```

**总计：** 约 110 行代码

---

### 方案 B 代码量

#### 前端代码（约 10 行）
```typescript
// frontend/src/contexts/MessagesContext.tsx

// 简单调用一个 API
const response = await apiGet('/api/v1/messages?page=1&page_size=100');

// 直接使用数据
const allMessages = (response.data?.items || []).map(transformBackendMessage);
setMessages(allMessages);

// ✅ 不需要：
// - 配置外部 FB API
// - 处理 FB 登录
// - 过滤和合并数据
// - 处理多个 API 错误
```

#### 后端代码（约 50 行）
```python
# backend/app/api/v1/messages.py

from app.clients.fb_message_client import FbMessageClient

@router.get("")
async def get_messages(
    page: int = 1,
    page_size: int = 100,
    db: AsyncSession = Depends(get_db),
):
    """获取消息列表（自动合并本地 DB + 外部 FB API）"""

    # 1. 查询本地 DB
    query = select(Message).offset(...).limit(...)
    result = await db.execute(query)
    local_messages = result.scalars().all()

    # 2. 并行获取 FB 已发送消息
    try:
        fb_client = FbMessageClient()
        fb_sent_messages = await fb_client.get_sent_messages()
    except Exception as e:
        logger.error(f"FB API 调用失败: {e}")
        fb_sent_messages = []  # 降级处理

    # 3. 合并返回
    all_messages = [
        *[msg.to_dict() for msg in local_messages],
        *fb_sent_messages,
    ]

    return {"data": {"items": all_messages}}
```

#### 配置文件
```bash
# backend/.env
FB_API_URL=https://api-youth-tycg.star-bit.io
FB_FIRM_ACCOUNT=tycg-admin
FB_FIRM_PASSWORD=123456  # ✅ 只在后端，用户看不到

# frontend/.env.development
# ✅ 不需要配置 FB 相关
```

**总计：** 约 60 行代码

---

## 📊 代码量对比

| 项目 | 当前方案 | 方案 B | 减少 |
|------|---------|--------|------|
| 前端代码 | ~80 行 | ~10 行 | **87.5%** ↓ |
| 后端代码 | ~30 行 | ~50 行 | 66% ↑ |
| 前端配置 | 3 项 | 0 项 | **100%** ↓ |
| 总代码量 | ~110 行 | ~60 行 | **45%** ↓ |

**结论：** 方案 B 总体代码量减少 45%，前端代码减少 87.5%！

---

## 🔐 安全性对比

### 当前方案
```javascript
// 构建后的前端代码（用户可以在浏览器看到）
const fbApiBaseUrl = "https://api-youth-tycg.star-bit.io";
const fbFirmAccount = "tycg-admin";
const fbFirmPassword = "123456";  // ⚠️ 明文密码！
```

**风险：**
- ❌ 任何人打开浏览器开发者工具都能看到密码
- ❌ 爬虫可以从 JS 文件中提取密码
- ❌ 如果有人获得密码，可以直接调用 FB API

### 方案 B
```python
# 后端代码（用户完全看不到）
FB_FIRM_PASSWORD: str  # 从 .env 读取，只在服务器
```

**优点：**
- ✅ 密码只在服务器端，用户永远看不到
- ✅ 前端代码完全不包含敏感信息
- ✅ 即使前端被攻击，也无法获取密码

---

## 🚀 性能对比

### 当前方案
```
用户访问列表
  ↓
前端并行调用 2 个 API
  ├─→ 本地 API: 50ms
  └─→ 外部 FB API: 2000ms  ← 慢！
  ↓
等待最慢的 API (2000ms)
  ↓
前端合并数据 (10ms)
  ↓
总耗时: 2010ms
```

### 方案 B（无缓存）
```
用户访问列表
  ↓
前端调用 1 个 API
  ↓
后端并行处理
  ├─→ 查询 DB: 50ms
  └─→ 调用 FB API: 2000ms
  ↓
后端合并: 5ms
  ↓
返回前端: 10ms
  ↓
总耗时: 2065ms (慢 55ms，可忽略)
```

### 方案 B（有缓存）
```
用户访问列表
  ↓
前端调用 1 个 API
  ↓
后端处理
  ├─→ 查询 DB: 50ms
  └─→ 读取缓存: 5ms  ← 超快！
  ↓
后端合并: 5ms
  ↓
返回前端: 10ms
  ↓
总耗时: 70ms (快 28 倍！)
```

**结论：**
- 无缓存时性能相近
- 有缓存时快 **28 倍**！

---

## 🎯 总结

### 当前方案
- 📝 前端代码复杂（80 行）
- ⚠️ 密码暴露在前端
- ⚪ 性能一般
- ❌ 无法缓存

### 方案 B
- ✅ 前端代码简单（10 行，减少 87.5%）
- ✅ 密码完全隐藏
- ✅ 可以缓存（性能提升 28 倍）
- ✅ 统一错误处理
- ✅ 更容易维护

**推荐：方案 B**
