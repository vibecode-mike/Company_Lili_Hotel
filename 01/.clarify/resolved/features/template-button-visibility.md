# 釐清問題
規格提到「圖片點擊型模板可選擇圖面下方是否顯示動作按鈕」，但缺少例子與行為描述；請確認按鈕顯示控制方式與預設值。

# 定位
Feature：spec/features/message_template.feature `Rule: 圖片點擊型模板規則：可選擇圖面下方是否顯示動作按鈕`（約第150-156行），目前僅標註 #TODO。

# 多選題
| 選項 | 描述 |
|------|------|
| A | 提供每張圖卡一個顯示/隱藏按鈕列的切換開關，預設顯示。 |
| B | 是否顯示按鈕由系統自動判斷（例如沒有設定互動動作時就隱藏），無需手動切換。 |
| C | 圖片點擊型模板的按鈕列永遠顯示，`#TODO` 可移除。 |
| Short | 其他（<=5字）。 |

# 影響範圍
決定前端表單欄位、預覽區渲染、JSON 產生與 LINE 模板行為，亦影響使用者操作教學與 QA 用例。

# 優先級
Medium

---
# 解決記錄

- **回答**：A - 內容管理者在 LINE Flex Message Simulator 中編輯配置項，視覺化預覽，產生 JSON
- **更新的規格檔**：spec/features/message_template.feature
- **變更內容**：
  1. 修正 Rule 名稱：「可選擇圖面下方是否顯示動作按鈕」→「按鈕顯示由 Flex Message 配置決定」
  2. 新增 Example 1：在 Flex Message Simulator 中配置按鈕，說明配置後會在視覺化預覽中顯示，並產生包含按鈕的 JSON
  3. 新增 Example 2：不配置按鈕時，預覽不顯示按鈕，產生不包含按鈕配置的 JSON
  4. 移除 #TODO 標記，補充完整的行為說明
