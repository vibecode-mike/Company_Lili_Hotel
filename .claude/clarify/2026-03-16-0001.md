# Clarify Session 2026-03-16

## Idea

我想要房型圖片能正確顯示，需要確認問題範圍與解決方案。

## Q&A

- Q: 圖片無法正確顯示的位置是哪裡？ → A: C — Chatbot 房型推薦卡片 + 後台 FAQ 管理介面都有問題
- Q: 目前房型圖片的現況是什麼？ → A: B — 上傳功能尚未實作，目前沒有圖片可用
- Q: 圖片的儲存方式偏好？ → A: A — 本地檔案系統，backend/kb/upload/ 目錄 + FastAPI 靜態路由
- Q: PMS 是否已串接？ → A: 理論上需要串接但實際實作狀態不確定，方案需同時支援 PMS 與 FAQ 靜態資料兩種來源
- Q: booking_billing.json 為何是 JSON 檔？ → A: 用戶指出應從 DB 讀取。確認：_kb_search() 已從 DB 讀取（FaqRule 表），kb_sync.py 只是額外同步快照。問題在於 image_url 欄位在 DB 中如何儲存與上傳
- Q: 需求範圍？ → A: A — 完整方案：後台 FAQ 管理加圖片上傳 → 存本地 → image_url 寫入 FaqRule DB → Chatbot 卡片 & 後台都能正確顯示
- Q: 後台圖片上傳操作方式？ → A: B — 上傳 + URL 並存，保留 URL 輸入框，額外加上傳按鈕，上傳成功後自動填入 URL
- Q: image_url 為空時的 fallback？ → A: A — 使用 DEFAULT_ROOM_IMAGE_URL 環境變數指定預設圖（規格原設計）
- Q: 上傳圖片尺寸限制？ → A: A — 最大 5MB，後端自動壓縮/縮放至寬度上限 1200px，格式統一 JPEG
